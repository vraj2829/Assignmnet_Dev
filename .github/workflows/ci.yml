name: Django CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Django setup
        run: |
          source venv/bin/activate
          python app.py || true

      - name: Verify Django configuration
        shell: bash
        run: |
          source venv/bin/activate
          python -c "import django; from django.conf import settings; print('✅ Django loaded successfully with DEBUG =', settings.DEBUG)"

      - name: Build Docker image
        run: |
          docker build -t django-app .

      - name: Complete
        run: echo "🚀 CI pipeline finished successfully!"
